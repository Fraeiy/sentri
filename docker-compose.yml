services:
  sentri:
    build:
      context: .
      dockerfile: docker/Dockerfile
    container_name: sentri
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - tg-data:/data
    # Interactive auth must be done first:
    #   docker compose run --rm sentri auth
    # Then start watching:
    #   docker compose up -d
    command: watch

volumes:
  tg-data:
    driver: local